runtime: java17 # or another supported version

instance_class: F2

handlers:
  - url: /.*
    secure: always
    redirect_http_response_code: 301
    script: auto

env_variables:
  db_name: $DB_NAME
  db_user: $DB_USERNAME
  db_pass: $DB_PASSWORD
